package com.rocka.concurrent;

import org.junit.Test;

/**
 * @describtion ThreadLocal 是线程的局部变量，提供get/set对这个局部变量进行操作，不会对其他线程有干扰，实现了线程数据隔离
 */
public class ThreadLocalTest {
    @Test
    public void testLocal(){

    }
}
